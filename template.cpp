#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    // Input
    int n;
    cin >> n;

    // Solve problem here
    cout << n << endl;

    return 0;
}
